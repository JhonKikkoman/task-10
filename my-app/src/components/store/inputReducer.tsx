/** @format */

const initialState = { textValue: '', numberValue: '', id: '' };

export const inputReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
